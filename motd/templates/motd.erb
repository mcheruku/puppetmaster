#########################
this server connect with puppet server
some files always replace with puppet, so ucant so anything
<%= @player1 %> is <%= @hostname %> good movie to get inspired....
############################################
